{
  "scripts": {
    "canais": "scp scraper.js package.json package-lock.json series.json m3u-catalog.html test-stream.html ubuntu@164.152.62.129:~/canais/ && scp -r templates series ubuntu@164.152.62.129:~/canais/ && ssh ubuntu@164.152.62.129 \"bash -lc 'cd ~/canais && npm install --omit=dev && pm2 restart scraper'\"",
    "release:apk": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/publish-release.ps1"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "multer": "^2.3.0",
    "node-cron": "^4.6.0",
    "puppeteer": "^25.10.0"
  }
}
